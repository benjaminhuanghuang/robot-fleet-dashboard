# Todos

## upgrade to a "Robot Fleet"?

- Add a `robotId` to each robot.
- The backend maintains a `Map<robotId, rosConnection>`.
- Add a robot list to the frontend.

## Adding maps and visualization
